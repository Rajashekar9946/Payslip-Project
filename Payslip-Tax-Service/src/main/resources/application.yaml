# Database Configuration
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/PayslipEmployeeServiceDB
    username: postgres
    password: Rajashekar@123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

# Server Configuration
server:
  port: 9092